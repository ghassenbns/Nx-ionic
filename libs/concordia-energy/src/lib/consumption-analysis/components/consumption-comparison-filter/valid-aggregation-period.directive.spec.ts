import { ValidAggregationPeriodDirective } from './valid-aggregation-period.directive';

describe('ValidAggregationPeriodDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidAggregationPeriodDirective();
    expect(directive).toBeTruthy();
  });
});
